generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("postgresql://neondb_owner:npg_7GQ9mjfHcALS@ep-green-boat-ad771gvz-pooler.c-2.us-east-1.aws.neon.tech/neondb?channel_binding=require&connect_timeout=15&sslmode=require")     // Usa Pooling para las consultas rápidas de tu app
  directUrl = env("postgresql://neondb_owner:npg_7GQ9mjfHcALS@ep-green-boat-ad771gvz.c-2.us-east-1.aws.neon.tech/neondb?channel_binding=require&sslmode=require") // Conexión directa (necesaria para migraciones)
}

model Product {
  id          Int      @id @default(autoincrement())
  title       String
  description String?  @db.Text
  category    String   // "remeras", "pantalones", "gorras", "conjuntos", "zapatillas", "abrigo", "accesorios"
  price       Float
  images      String   @db.Text // JSON string of image URLs
  isFeatured  Boolean  @default(false)
  createdAt   DateTime @default(now())
}
